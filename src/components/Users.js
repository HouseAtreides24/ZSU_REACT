function Users({id,name,username}){

    return(
        <div>
            <div>{id}</div>
            <div>{name}</div>
            <div>{username}</div>
        </div>
    )
}

export default Users;
